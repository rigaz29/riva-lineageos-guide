.claude/

# Artefak ROM (hardlink ke out/, ~1 GB each) — jangan pernah masuk git
rom/*.zip
rom/*.img
rom/*.sha256

# Artefak perangkat / GApps / log — jangan masuk repo
*.tar.gz
*_bugreport_*
*_install_log_*
wget-log*
BiTGApps-*.zip
